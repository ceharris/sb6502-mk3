MEMORY {
    ROM: start=$0, size=$80000, fill=yes, fillval=$EA;
}

SEGMENTS {
    MONITOR: load=ROM, type=ro, offset=$0000;
    BASIC: load=ROM, type=ro, offset=$1000;  # covers $1000-$3FFF
    SNAKE: load=ROM, type=ro, offset=$4000;
    BIOS: load=ROM, type=ro, offset=$7000;
}
